{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "spinner",
  "type": "registry:ui",
  "title": "Spinner",
  "description": "An activity indicator painted with the resolved theme colour.",
  "author": "Alejo Garcia",
  "categories": [
    "feedback"
  ],
  "registryDependencies": [
    "https://alejo-garcia.github.io/templapp-registry/r/theme.json"
  ],
  "files": [
    {
      "path": "src/components/ui/spinner.tsx",
      "type": "registry:ui",
      "content": "import { ActivityIndicator } from 'react-native';\n\nimport { useThemeColors } from '@/theme';\n\nexport function Spinner({ size = 'small', color }: { size?: 'small' | 'large'; color?: string }) {\n  const colors = useThemeColors();\n  return <ActivityIndicator size={size} color={color ?? colors.primary} />;\n}\n"
    }
  ],
  "meta": {
    "surface": "native"
  }
}
